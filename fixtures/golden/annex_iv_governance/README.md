# Annex IV Governance Scenario

Checked-in source fixtures for the audit-ready Annex IV governance acceptance path.

Scenario:

- system: `hiring-assistant`
- model: `hiring-model-v3`
- version: `2026.03`
- role: `provider`
- domain: employment

These files are intentionally source-first:

- governance item payloads use the canonical evidence field names
- `expected_full_manifest_summary.json` records the expected full-pack inclusion order
- `expected_disclosure_summary.json` records the expected disclosure selectors and no-byte artefact policy

The pack archives themselves are generated by tests and examples rather than checked in.
